# Changelog

## 1.0.0

- Versão inicial.
- `GeoPoint` com validação de limites geográficos.
- `haversineDistanceMeters` para distância entre coordenadas.
- `formatCoordinates` e `formatDistance` com saída em pt-BR.
- `Weather` e `WeatherClient` para consumo da API REST do OpenWeather.
- 24 testes unitários cobrindo todos os módulos.
